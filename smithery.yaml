version: '1'
build:
  type: 'node'
  version: '18'
  command: 'npm install'

run:
  command: 'node index.js'
  port: 3001
  env:
    - name: PORT
      value: '3001'
    - name: OPENWEATHER_API_KEY
      secret: true

mcp:
  manifest_path: '/mcp-manifest.json' 